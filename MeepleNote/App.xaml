<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MeepleNote"
             x:Class="MeepleNote.App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Colores principales -->
            <Color x:Key="PrimaryColor">#1E3A8A</Color>
            <!-- Azul Oscuro -->
            <Color x:Key="AccentColor">#FACC15</Color>
            <!-- Amarillo Dorado -->
            <Color x:Key="AlertColor">#F87171</Color>
            <!-- Rojo Suave -->
            <Color x:Key="SecondaryColor">#86EFAC</Color>
            <!-- Verde Pastel -->
            <Color x:Key="BackgroundColor">#F9FAFB</Color>
            <!-- Blanco Roto -->

            <!-- Estilos reutilizables -->
            <Style x:Key="PrimaryButtonStyle" TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource AccentColor}" />
                <Setter Property="TextColor" Value="Black" />
                <Setter Property="CornerRadius" Value="12" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="HeightRequest" Value="50" />
                <Setter Property="Margin" Value="10"/>
            </Style>

            <Style x:Key="TitleLabelStyle" TargetType="Label">
                <Setter Property="FontSize" Value="28" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="Margin" Value="20"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
